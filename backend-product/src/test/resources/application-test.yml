spring:
  datasource:
    url: jdbc:h2:mem:dwkshop-product;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  data:
    elasticsearch:
      repositories:
        enabled: false
dwkshop:
  search:
    elasticsearch:
      enabled: false
